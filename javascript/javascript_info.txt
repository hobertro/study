How is Javascript related to Java?; It isn't
In what environments can Javascript be excuted? Browser? Server? Both?; Both
What does setTimeout do?; setTimeout allows us to run a function once after the interval of time
What does setInterval do?; Allows us to run a function repeatedly, starting after the interval of time, then repeating continuously at that interval.
How does a nested setTimeout more flexible than setInterval?; Nested setTimeout allows to set the delay between the executions more precisely than setInterval, for example, changing the the time the function will run next or adding
What is the difference between Math.ceil and Math.floor?; The Math.ceil() function always rounds a number up to the next largest integer while The Math.floor() function returns the largest integer less than or equal to a given number.
